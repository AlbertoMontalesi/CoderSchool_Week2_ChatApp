<h1>Messages#incoming</h1>
<p>Find me in app/views/messages/incoming.html.erb</p>
<p> You have <%= @incoming_messages.size %> messages  </p>


<div class="row">
    <div class="col-xs-12 col-sm-6">
        <%= link_to "New Message", new_message_path %>
    </div>
     <div class="col-xs-12 col-sm-6">
        <%= link_to "Add Friends", users_path %>
     </div>


</div>







<% @incoming_messages.each do |message| %>
    <div class="row">
        <div class="col-xs-12">
            From <%= message.sender %> To <%= message.recipient %>  
        </div>
        <div class="col-xs-12">
            <%= message.subject %>
        </div>
        <div class="col-xs-12">
            <%= message.body %>
        </div>    
    </div>


<% end %>
<!-- 
<table>
    <thead>
        <tr>
            <th>From</th>
            <th>To</th>
            <th>Subject</th>
            <th>Body</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <% @incoming_messages.each do |message| %>
                <td> <%= message.sender %> </td>
                <td> <%= message.recipient %> </td>
                <td> <%= message.subject %> </td>
                <td> <%= message.body %> </td>     
            <% end %>
        </tr>    
    </tbody>
</table>
-->
